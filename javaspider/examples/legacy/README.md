# JavaSpider Legacy Examples

These Java files are retained as source-only legacy references.

They are not part of the production Maven source root and they must not be used
as the canonical public surface.

Canonical public entrypoints:

- `com.javaspider.EnhancedSpider`
- `com.javaspider.cli.SuperSpiderCLI`

Rules:

- keep these files marked `@Deprecated`
- do not add new framework features here
- prefer unified CLI examples in active docs
