fn main() {
    eprintln!("Legacy example moved to examples/legacy/cluster_worker.rs");
    eprintln!(
        "Run the legacy example directly if you specifically need the old distributed worker demo."
    );
}
