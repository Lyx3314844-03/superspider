fn main() {
    eprintln!("Legacy example moved to examples/legacy/dynamic_crawler.rs");
    eprintln!(
        "Use `rustspider browser fetch --url <url>` for the canonical dynamic crawling path."
    );
}
