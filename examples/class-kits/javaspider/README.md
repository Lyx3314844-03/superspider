# JavaSpider Class Kit

Copy these factory classes into a JavaSpider project and call `ClassKitSpiders.register()` during project startup.
For marketplace work, start from the new `Ecommerce*SpiderFactory` classes plus `EcommerceSiteProfiles.java`.
