# PySpider Class Kit

Copy these files into a `spiders/` directory in a PySpider project and then customize selectors, URLs, and browser metadata.
For marketplace work, start from the new `ecommerce_*` templates plus `ecommerce_site_profile.py`.
