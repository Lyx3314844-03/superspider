# RustSpider Class Kit

Copy these modules into a RustSpider project and call `register_catalog()` from your spider registry.
For marketplace work, start from the new `EcommerceCrawler` wrapper, the `ecommerce_*` templates, and `ecommerce_profile.rs`.
