# GoSpider Class Kit

Copy these files into a GoSpider project package and register them through `RegisterCatalog()`.
For marketplace work, start from the new `ecommerce_*` templates plus `ecommerce_profile.go`.
