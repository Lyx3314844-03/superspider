# GoSpider Class Kit

Copy these files into a GoSpider project package and register them through `RegisterCatalog()`.
For marketplace work, start from the new `EcommerceCrawler` wrapper, the `ecommerce_*` templates, and `ecommerce_profile.go`.
