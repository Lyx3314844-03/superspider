# Node Control Plane Client

Minimal external integration example for the shared `/api/tasks` contract.

## Usage

```bash
node client.mjs --base http://localhost:5000/api/tasks --url https://example.com
```
