# Legacy GoSpider Examples

These files were moved out of the root `examples/` directory because they are no longer the primary examples for the framework:

- `cluster_worker.go`
- `distributed_main.go`
- `download_youku.ps1`
- `install_ffmpeg.py`
- `merge_video.py`

They remain for historical and operator reference only.
